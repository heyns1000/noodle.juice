# SeedwaveCore™ — Fruitful Ecosystem Dependencies
# Generated: 2026-06-26

# Web framework (webhook servers)
flask>=3.0.0,<4.0.0

# HTTP client (R2 / Cloudflare API calls)
requests>=2.31.0,<3.0.0

# AWS-compatible S3 client for Cloudflare R2
boto3>=1.34.0,<2.0.0

# Async support (optional, for future Cloudflare Workers bridge)
httpx>=0.27.0,<1.0.0

# Data validation
pydantic>=2.6.0,<3.0.0

# Type hints backport (Python 3.9 compat)
typing-extensions>=4.9.0

# Cryptography (HMAC + SHA-256 — already stdlib, kept for explicit declaration)
# No extra package needed: hashlib, hmac are stdlib

# Development / testing (optional)
pytest>=8.0.0
pytest-asyncio>=0.23.0
